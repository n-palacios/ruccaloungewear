<odoo>
  <data>
    <!-- explicit list view definition -->

    <record model="ir.ui.view" id="stories.collection">
      <field name="name">Collections</field>
      <field name="model">stories.collection</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="launch_date"/>
        </tree>
      </field>
    </record>

    <!-- actions opening views on models -->
    <record model="ir.actions.act_window" id="stories.action_window">
      <field name="name">Collection window</field>
      <field name="res_model">stories.collection</field>
      <field name="view_mode">tree,form</field>
    </record>

    <!-- server action to the one above -->
    <record model="ir.actions.server" id="stories.action_server">
      <field name="name">Stories/Collection server</field>
      <field name="model_id" ref="model_stories_collection"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>

    <!-- Top menu item -->

    <menuitem name="Menu" id="stories.menu_root"/>

    <!-- menu categories -->
    <menuitem name="Collection" id="stories.collection" parent="stories.menu_root"/>
    <menuitem name="Stories" id="stories.story" parent="stories.menu_root"/>
    <menuitem name="Serial codes" id="stories.serials" parent="stories.menu_root"/>
    <!-- actions -->
    <!--
    <menuitem name="List" id="stories.menu_1_list" parent="stories.collection"
              action="stories.action_window"/>
    <menuitem name="Server to list" id="my_module" parent="stories.menu_2"
              action="stories.action_server"/>
     -->
  </data>
</odoo>